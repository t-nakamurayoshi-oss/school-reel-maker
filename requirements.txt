# school-reel-maker の依存ライブラリ一覧
# インストール方法: pip install -r requirements.txt

# 動画の読み込み・編集・書き出しに使うライブラリ
# MoviePy は内部で ffmpeg を使って動画を処理します
moviepy==1.0.3

# 音量計算など数値処理に使うライブラリ
numpy>=1.21.0

# MoviePy が内部で使う画像 I/O ライブラリ
imageio>=2.9.0

# ffmpeg のバイナリを Python パッケージとして同梱するライブラリ
# これにより、ffmpeg を別途インストールしなくても動く場合があります
imageio-ffmpeg>=0.4.7

# YAML ファイルの読み込みに使うライブラリ
# presets/variants.yaml を読み込む make_reel_ab.py で必要
pyyaml>=6.0

# ============================================================
# オプション: スコアリングエンジン用ライブラリ
# 以下はインストールしなくても make_reel.py / make_reel_ab.py は動きます。
# 高度なスコアリングを使う場合にインストールしてください。
# ============================================================

# 顔検出・笑顔検出・人物中心クロップに使うライブラリ
# インストールすると score_engine.py の笑顔スコア・顔サイズスコアが有効になる
# headless 版は GUI 不要でサーバー・CLI 環境向け（推奨）
# extract_talking.py の実行にも必須
opencv-python-headless>=4.5.0

# 音声文字起こし（文脈スコア）に使うライブラリ
# インストール後、presets/variants.yaml の use_whisper: true に変更して使う
# 初回実行時にモデルファイル(39MB〜)がダウンロードされます
# インストール: pip install openai-whisper
# openai-whisper
